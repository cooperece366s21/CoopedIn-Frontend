const initialState = {
    activePage: "Login",
    domainName: "http://localhost:8080", 
    loggedIn: "false", 
    username: ""
};

// function rootReducer(state = initialState, action) => {
//     switch(action.type) {

//     }
// }